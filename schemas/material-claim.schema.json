{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SEO Material Claim",
  "description": "A material factual, numeric, URL, policy, comparative, or completion claim bound to evidence.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "claim_id",
    "claim_type",
    "statement",
    "bound_fields",
    "evidence_refs",
    "evidence_state",
    "inference"
  ],
  "properties": {
    "claim_id": {"type": "string", "minLength": 1, "pattern": ".*\\S.*"},
    "claim_type": {
      "type": "string",
      "enum": ["numeric", "url", "date", "platform_behavior", "policy", "legal", "comparative", "completion", "other"]
    },
    "statement": {"type": "string", "minLength": 1, "pattern": ".*\\S.*"},
    "bound_fields": {
      "type": "array",
      "description": "Exact material output field paths whose values this claim supports.",
      "items": {"type": "string", "minLength": 1, "pattern": ".*\\S.*"},
      "minItems": 1,
      "uniqueItems": true
    },
    "normalized_value": {},
    "unit": {"type": "string"},
    "evidence_refs": {
      "type": "array",
      "items": {"type": "string", "minLength": 1, "pattern": ".*\\S.*"},
      "minItems": 1,
      "uniqueItems": true
    },
    "evidence_state": {
      "type": "string",
      "enum": ["AVAILABLE", "PARTIAL", "MISSING", "STALE", "INVALID", "BLOCKED"]
    },
    "inference": {"type": "boolean"}
  }
}
